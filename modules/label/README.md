# label

Vendored copy of [cloudposse/terraform-null-label](https://github.com/cloudposse/terraform-null-label)
at tag `0.25.0` (commit `488ab91e34a24a86957e397d9f7262ec5925586a`).

Vendored in-repo instead of pulled from the public Terraform Registry to remove the
runtime dependency on an external module source. Used only by
`modules/permission-sets` and `modules/account-assignments`.

To pick up an upstream update, re-vendor from the desired tag and diff against this copy.
